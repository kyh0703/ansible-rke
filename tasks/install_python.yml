---
- name: Install all required dependencies
  yum:
    name: "{{ item }}"
    state: present
  with_items:
    - "epel-release"
    - "python3"
    - "python3-devel"
    - "python3-pip"
    - "python-argparse"

- name: Install Required Python Library
  shell: "pip3 install pyyaml"
